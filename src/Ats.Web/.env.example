# Base URL of the Ats API. The dev API listens on http://localhost:5236
# (see src/Ats.Api/Properties/launchSettings.json). Copy this file to `.env` and adjust per machine.
# Only VITE_-prefixed variables are exposed to client code.
VITE_API_BASE_URL=http://localhost:5236
